# ═══════════════════════════════════════════════════════════════════════════
# YOLO Hyperparameter Benchmark System - Dependencies
# ═══════════════════════════════════════════════════════════════════════════
#
# Project: Influência de Hiperparâmetros no Treinamento do YOLO (UNIP, 2025)
#
# Installation:
#   pip install -r requirements.txt
#
# For GPU support (CUDA):
#   Install PyTorch with CUDA support first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# For Apple Silicon (MPS):
#   PyTorch with MPS support comes by default in recent versions
#
# ═══════════════════════════════════════════════════════════════════════════

# Core Deep Learning Framework
# torch>=2.0.0
# torchvision>=0.15.0

# YOLO Implementation
# ultralytics>=8.0.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Configuration and Validation
pydantic>=2.0.0
pyyaml>=6.0

# CLI and Visualization (Rich library - comprehensive)
rich>=13.0.0

# Interactive Visualizations (Bokeh)
bokeh>=3.0.0

# System Monitoring
psutil>=5.9.0
memory-profiler>=0.60.0

# Testing (for development)
pytest>=7.0.0
pytest-cov>=4.0.0

# Code Quality (optional, for development)
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.0.0

# ═══════════════════════════════════════════════════════════════════════════
# Version Notes
# ═══════════════════════════════════════════════════════════════════════════
#
# - ultralytics: YOLOv8 official implementation
#   Includes training, validation, and inference capabilities
#
# - pydantic>=2.0.0: Required for configuration validation
#   Note: v2 has breaking changes from v1
#
# - rich>=13.0.0: For beautiful CLI with progress bars, tables, panels
#   One of the most comprehensive CLI libraries
#
# - bokeh>=3.0.0: For interactive HTML visualizations
#   Provides professional-quality plots with hover tools
#
# - psutil: For real-time system resource monitoring
#
# - memory-profiler: For detailed memory usage tracking
#
# ═══════════════════════════════════════════════════════════════════════════
# Platform-Specific Notes
# ═══════════════════════════════════════════════════════════════════════════
#
# Linux/Windows with NVIDIA GPU:
#   1. Install CUDA Toolkit (11.8 or higher recommended)
#   2. Install PyTorch with CUDA:
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   3. Install remaining requirements:
#      pip install -r requirements.txt
#
# macOS (Apple Silicon M1/M2):
#   1. PyTorch with MPS (Metal Performance Shaders) support:
#      pip install torch torchvision
#   2. Install remaining requirements:
#      pip install -r requirements.txt
#
# CPU Only:
#   pip install -r requirements.txt
#   (Will use CPU for training - significantly slower)
#
# ═══════════════════════════════════════════════════════════════════════════
# Recommended System Requirements
# ═══════════════════════════════════════════════════════════════════════════
#
# Minimum (CPU):
#   - CPU: 4+ cores
#   - RAM: 16GB
#   - Storage: 50GB free
#   - Time per epoch: ~10-30 minutes (varies by dataset)
#
# Recommended (GPU):
#   - GPU: NVIDIA RTX 3060 or better (8GB+ VRAM)
#   - CPU: 8+ cores
#   - RAM: 32GB
#   - Storage: 100GB free (SSD recommended)
#   - Time per epoch: ~1-5 minutes
#
# High-Performance (for large benchmarks):
#   - GPU: NVIDIA RTX 4090 or A100 (24GB+ VRAM)
#   - CPU: 16+ cores
#   - RAM: 64GB+
#   - Storage: 500GB+ NVMe SSD
#   - Time per epoch: <1 minute
#
# ═══════════════════════════════════════════════════════════════════════════
